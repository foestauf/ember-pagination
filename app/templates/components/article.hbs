Hello
    <button type="button" {{on "click" this.toggleModal}}
        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Toggle Modal</button>

{{#if this.isShowingModal}}
<Modal-Dialog onClose="this.toggleModal" targetAttachment="center"
    clickOutsideToClose="true"
    translucentOverlay="true">

    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" type="button"
        {{on "click" this.toggleModal}}>Close</button>
             Title:   {{@model.title}}
        <p>
        {{@model.body}}
        </p>
Hey you guys
</Modal-Dialog>
{{/if}}

{{yield}}

