    <button type="button" {{on "click" this.toggleModal}}
        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Toggle Modal</button>

{{#if this.isShowingModal}}
<ModalDialog
    @onClose={{this.toggleModal}}
    @clickOutsideToClose={{true}}
    @translucentOverlay={{true}}
    @overlayPosition='sibling'
    @overlayClass="centered-scrolling-overlay"
    @wrapperClass="centered-scrolling-wrapper"
 >
    <div>
        by: {{@model.user.name}}({{@model.user.email}})
    </div><button type="button" {{on "click" this.toggleModal}}>
    <i class="fal fa-2x fa-window-close absolute top-0 right-0 h-8 w-8 m-1"></i>
    </button>

             Title:   {{@model.title}}
        <p>
        {{@model.body}}
        </p>

</ModalDialog>
{{/if}}

{{yield}}

